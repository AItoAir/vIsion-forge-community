# README media assets

This folder stores lightweight derivatives used by repository documentation.

- Keep raw screen recordings and source exports outside Git history.
- Prefer animated GIF previews and compressed MP4 derivatives for README usage.
- Keep poster images only when a static fallback thumbnail is still needed.
- If a future demo needs the original high-resolution binary, publish it as a
  GitHub release asset instead of committing it to the repository.
