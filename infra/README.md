# Infrastructure Layout

The supported public compose layout for FramePin Community Edition is:

- `compose.base.yaml`
- `compose.cpu.yaml`
- `compose.gpu.yaml`
- `compose.cloud.yaml`

Use those files through `manage_frame_pin.sh` or `manage_frame_pin.bat`
with `LF_RUNTIME_PROFILE`.
