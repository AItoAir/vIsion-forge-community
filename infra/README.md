# Infrastructure Layout

The supported public compose layout for VisionForge Community Edition is:

- `compose.base.yaml`
- `compose.cpu.yaml`
- `compose.gpu.yaml`
- `compose.cloud.yaml`

Use those files through `manage_vision_forge.sh` or `manage_vision_forge.bat`
with `LF_RUNTIME_PROFILE`.
