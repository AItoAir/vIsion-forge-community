# Infrastructure Layout

The supported public compose layout for Label-Forge Community Edition is:

- `compose.base.yaml`
- `compose.cpu.yaml`
- `compose.gpu.yaml`
- `compose.cloud.yaml`

Use those files through `manage_label_forge.sh` or `manage_label_forge.bat`
with `LF_RUNTIME_PROFILE`.
